# Output Quality Criteria

Use this criterion to evaluate the generated task or test artifacts.

## Questions

- Does each task start from the configured entry URL?
- Are navigation steps phrased as real UI actions instead of guessed direct URLs?
- Do assertions match the page type?
- Are overlay workflows validated by visible UI evidence instead of URL only?
- Are login requirements and storage state metadata applied correctly?
